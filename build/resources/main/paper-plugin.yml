name: Legacy
version: '1.0'
main: me.comunidad.dev.legacy.Core
api-version: '1.21'
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
    ProtocolLib:
      load: BEFORE
      required: true